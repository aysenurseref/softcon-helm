apiVersion: v1
kind: Service
metadata:
  name: {{ include "softconAPI.fullname" . }}
  labels:
    {{- include "softconAPI.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "softconAPI.selectorLabels" . | nindent 4 }}
